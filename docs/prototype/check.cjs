const {chromium}=require('playwright');
(async()=>{const browser=await chromium.launch({headless:true,...(process.env.CHROME_BIN?{executablePath:process.env.CHROME_BIN}:{})});const page=await browser.newPage({viewport:{width:700,height:950}});const errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.goto(require('node:url').pathToFileURL(require('node:path').resolve(process.argv[2])).href);
const f=page.frameLocator('iframe'),btn=(name)=>f.getByRole('button',{name,exact:true}),click=async n=>btn(n).click(),text=async()=>f.locator('#tennis-screen').innerText();
const assert=(v,msg)=>{if(!v)throw Error(msg)};
await click('Открыть');assert(!(await text()).includes('+5 ₽'),'submenu should be separate');await click('Присоединиться');await click('Время · 0 ч');await click('+1 ч');await click('⬅️ Назад');await click('Оплата · 0 ₽');
for(const n of ['+5 ₽','+50 ₽','+100 ₽','−5 ₽','−50 ₽','−100 ₽'])assert(await btn(n).count()===1,n);
await click('+50 ₽');await click('+5 ₽');await click('⬅️ Назад');await click('Добавить гостя');await click('Добавить гостя');assert((await text()).includes('Алексей гость 2'),'guests');await click('Не участвую');assert((await text()).includes('Оплата · 55 ₽'),'payment preserved');await click('Закрыть');assert(await btn('Открыть').count()===1,'closed panel');
await click('У бота');await click('➕ Создать тренировку');await f.getByRole('textbox',{name:'Ответ'}).fill('Тест создателя');await click('Отправить');await click('12.09.2026');await click('19:00');await click('Опубликовать в группе');assert((await text()).includes('Учесть тренировку'),'creator can finish');
await f.locator('#tennis-role').selectOption('admin');await click('📋 Управление тренировками');await f.getByRole('button',{name:/Тест создателя/}).click();await click('Игроки');await click('Участники группы');await f.getByRole('button',{name:/▫️ Борис/}).click();await click('Добавить · 1');await f.getByRole('button',{name:/Борис · 0 ч/}).click();await click('+0,5 ч');await click('Платил · 300 ₽');await click('Всё правильно');
await f.locator('#tennis-role').selectOption('user');await click('🏓 Мои тренировки');await f.getByRole('button',{name:/Тест создателя/}).click();await click('🧮 Учесть тренировку');await click('✅ Подтвердить учёт');await click('В меню группы');await click('🏓 Мои тренировки');assert(!(await text()).includes('Тест создателя'),'closed non-playing creator should not see training');
await click('В меню группы');await click('💰 Мои расчёты');await click('💸 Записать перевод');await click('Борис');await click('Я отправил → Борис');await f.getByRole('textbox',{name:'Ответ'}).fill('125');await click('Отправить');await click('Деньги переданы — записать');await click('Уточнить перевод');assert((await text()).includes('Уточняем'),'transfer review');await click('Всё верно');await click('✏️ Исправить сумму');await f.getByRole('textbox',{name:'Ответ'}).fill('150');await click('Отправить');await click('✅ Сохранить сумму');assert((await text()).includes('150 ₽'),'transfer correction');
await f.locator('#tennis-role').selectOption('super');await click('👥 Администраторы бота');await click('Борис · Админ бота');await click('Снять назначение');assert((await text()).includes('Участник · административных прав нет'),'remove role');await click('Назначить администратором');assert((await text()).includes('Администратор бота · назначен'),'grant role');
await click('Назад');await click('В меню группы');await click('➕ Создать тренировку');await f.getByRole('textbox',{name:'Ответ'}).fill('Не сохранено');await click('Отмена');assert((await text()).includes('Есть несохранённые изменения'),'exit guard');await click('Продолжить ввод');assert(await f.getByRole('textbox',{name:'Ответ'}).count()===1,'resume');
await click('Отмена');await click('Сбросить и выйти');await click('В группе');await page.screenshot({path:require('node:path').join(require('node:os').tmpdir(),'tennis-prototype-desktop.png')});await page.setViewportSize({width:360,height:850});await page.screenshot({path:require('node:path').join(require('node:os').tmpdir(),'tennis-prototype-mobile.png')});
assert(errors.length===0,errors.join('\n'));console.log('PASS: participation, guests, payment, creator close/filter, admin editor, transfers, role assignments, exit guard; no browser errors');await browser.close();})();
